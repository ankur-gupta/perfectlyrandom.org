gem "github-pages", group: :jekyll_plugins
gem "csv"  # Jekyll needs csv but it has been removed from stdlib. See https://github.com/jekyll/jekyll/blob/v3.9.3/lib/jekyll.rb#L28.
gem 'webrick'  # Needed to run `bundle exec jekyll serve`
